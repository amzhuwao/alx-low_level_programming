#include "main.h"
#include <stdio.h>

/**
 * mul - gives the multiple of variable a & b
 * @a: local variable
 * @b: local variable
 * Return: a times b
 */

int mul(int a, int b)
{
	return (a * b);
}
